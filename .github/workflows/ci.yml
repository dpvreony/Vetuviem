name: CI .NET

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    permissions:
      actions: write
      contents: write
      deployments: read
      pages: write
      id-token: write
      pull-requests: write
      packages: read
      security-events: write
    uses: dpvreony/github-action-workflows/.github/workflows/dotnet-ci.yml@dab8aec7b420664cb5c2100d0982440a5b476c15
    with:
      buildOs: "windows"
      solutionName: "Vetuviem"
    secrets:
      NUGET_USER: ${{ secrets.NUGET_USER }}
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

